'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
const items=['Problem','Customer','Market evidence','Offer','Pricing','Product or service','Brand and positioning','Acquisition strategy','Sales process','Financial model','AI systems','Customer feedback','90-day plan']
export default function Capstone(){const[done,setDone]=useState<boolean[]>(Array(13).fill(false));useEffect(()=>setDone(JSON.parse(localStorage.getItem('aisy_capstone')||JSON.stringify(Array(13).fill(false)))),[]);const toggle=(i:number)=>{const n=[...done];n[i]=!n[i];setDone(n);localStorage.setItem('aisy_capstone',JSON.stringify(n))};return <div className="shell"><nav className="nav"><Link className="brand" href="/">AI IS YOUR SUPERPOWER<span>.</span></Link><div className="navlinks"><Link href="/dashboard">Dashboard</Link><Link href="/curriculum">Curriculum</Link></div></nav><main className="section"><div className="eyebrow">FINAL PROJECT</div><h1>THE CAPSTONE</h1><p className="muted">This is not a certificate for watching videos. It is a business artifact. Build it, test it, improve it.</p><div className="checklist">{items.map((x,i)=><button className="checkitem" style={{display:'block',width:'100%',textAlign:'left',color:'inherit'}} onClick={()=>toggle(i)} key={x}><b>{done[i]?'✓':'○'} {String(i+1).padStart(2,'0')}</b> · {x}<p className="muted">Document the evidence, decision and next action.</p></button>)}</div><div className="quote" style={{marginTop:30}}>“If nobody wants it, don't fall in love with your idea.”<small>Operator principle</small></div><Link className="btn" href="/dashboard">Return to your build room →</Link></main></div>}