'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
const total=59
export default function Dashboard(){const[done,setDone]=useState<string[]>([]);useEffect(()=>{setDone(JSON.parse(localStorage.getItem('a i s y s_completed')||'[]'))},[]);const pct=Math.round(done.length/total*100);return <div className="shell dash"><nav className="nav"><Link className="brand" href="/">AI IS YOUR SUPERPOWER<span>.</span></Link><div className="navlinks"><Link href="/curriculum">Curriculum</Link><Link href="/journal">Journal</Link></div></nav><main className="dashHero"><div className="eyebrow">YOUR BUILD ROOM</div><h1>Stop collecting information.<br/><em>Start creating evidence.</em></h1><p className="lead muted">One lesson. One action. One step closer to something real.</p><div className="stats"><div className="stat"><strong>{pct}%</strong><span className="muted">course progress</span><div className="progressbar"><i style={{width:pct+'%'}}/></div></div><div className="stat"><strong>{done.length}</strong><span className="muted">lessons completed</span></div><div className="stat"><strong>{total-done.length}</strong><span className="muted">lessons remaining</span></div></div><div className="card"><div className="eyebrow">START HERE</div><h2>Money follows value.</h2><p className="muted">Before tools, tactics and technology, learn how customers decide what is worth paying for.</p><Link className="btn" href="/lesson/money-follows-value">Open lesson →</Link></div><section className="section"><div className="eyebrow">YOUR PATH</div><h2>Build. Sell. Scale.</h2><Link className="btn alt" href="/curriculum">Open full curriculum →</Link></section></main></div>}